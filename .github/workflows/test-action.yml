
name: Sample GitHub Action

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verification step by 23f2001912@ds.study.iitm.ac.in
        run: echo "GitHub Actions setup verified"
